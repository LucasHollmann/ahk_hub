# ahk_hub

Desktop workspace for creating and organizing AutoHotkey scripts, built with Next.js and Electron.

## Development

```bash
npm install
npm run dev
```

The web app is available at `http://localhost:3000`; `npm run dev` also starts the Electron shell.

## Production build

```bash
npm run dist
```
